<UserControl x:Class="EjaadTech.AVRonArduino.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             mc:Ignorable="d" 
             d:DesignHeight="67" d:DesignWidth="223"
             Name="MyToolWindow" FontSize="14">
             <!-- Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}" -->
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Margin="5 5" Text="COM Port"></TextBlock>
                <Button Margin="5 5" Content="SCAN" Name="bt_scan" Width="64" Click="bt_scan_Click" />
                <ComboBox  Margin="5 5" Height="23" Name="cbox_portList" Width="65" Text=""></ComboBox>
            </StackPanel>
            <!--Foreground="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowTextKey}}" -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Uno"  Margin="10 5" Width="60" Name="bt_uno" Click="button1_Click" />
                <Button Content="Nano" Margin=" 0 5" Width="60" Name="bt_nano" Click="button2_Click" />
                <Button Content="Mega" Margin="10 5" Width="60" Name="bt_mega" Click="button3_Click" />
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
